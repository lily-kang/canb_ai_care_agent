"""
FastAPI application wrapper for the CANB counseling sp_agent.

This package exposes HTTP endpoints (single and batch) that call the
underlying ADK-based root_agent defined in sp_agent.agent.
"""

